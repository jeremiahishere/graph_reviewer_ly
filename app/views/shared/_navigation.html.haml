- if user_signed_in?
  %ul{"class" => "tabs"}
    %li= link_to "Home", root_path
    - if can? :read, User
      %li= link_to "Manage Users", users_path
    - if can? :read, Graph 
      %li= link_to "Manage Graphs", graphs_path 
    - if can? :read, DisplayGraph 
      %li= link_to "Manage Display Graphs", display_graphs_path 
